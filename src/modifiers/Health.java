package modifiers;

/**
 * Modifier that heals the host Entity.
 * Developed by: Luis Marin
 */
public class Health extends Modifiers {

    /**
     * Creates a new Health Modifier.
     * @param points Points to heal.
     */
    public Health(int points) {
        super(points);
    }
}
