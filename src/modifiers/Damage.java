package modifiers;

/**
 * Modifier that modifies the damage to the host Entity.
 * Developed by: Luis Marin
 */
public class Damage extends Modifiers{

    /**
     * Creates a new Damage modifier.
     * @param points Points to modify.
     */
    public Damage(int points) {
        super(points);
    }
}
